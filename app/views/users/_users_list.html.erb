<% if @arrUsers.blank? %>
    There are no cleaners available in your area on this date.
<% else %>

<% @arrUsers.each do |user| %>
      <div class="card" style="width: 30%;">

        <!-- If user has an avatar, show it - otherwise show the default profile avatar -->
        <% if user.image.present? %>
        <%= link_to image_tag(user.image_url, class: 'card-img-top') %>
        <% else %>
        <%= image_tag('defaultprofile.svg', class: 'card-img-top')  %>
        <% end %>

          <div class="card-body center">
              <h4 class="card-title center text-teal"><%= link_to user.username, user %></h4>
              $<%= user.hourly_rate %>/hr
              <!-- Later add star ratings to show -->
              <!-- <div id="star_<%= user.id %>"></div> <%#= pluralize(user.average_rating, "review") %> -->
              <br/><br/>
              <%= link_to "VIEW", user_path(user.id), class: "btn center" %>
          </div>
          <br/>
      </div>
<% end %>

<% end %>
<!-- <script>
  $('#star_<%#= user.id %>').raty({
    path: '/assets',
    readOnly: true,
    score: <%#= user.average_rating %>
  });
</script> -->
